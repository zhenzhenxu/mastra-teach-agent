services:
  - type: web
    name: tech-mentor-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm run web
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: OPENAI_API_KEY
        sync: false
